query collectionNews($site: [String]!, $section: [String]!) {
  collectionNews: entries(site: $site, section: $section) {
    ... on news_contentBuilder_Entry {
      id
      title
      url
      uri
      typeHandle
      sectionHandle
      siteId
      slug
      entryShortDescription
    }
  }
}
