query entry($uri: [String]!, $site: [String]!, $section: [String]!) {
  entry(uri: $uri, site: $site, section: $section) {
    id
    title
    typeHandle
    sectionHandle
    siteId
    localized {
      siteId
      uri
    }
  }
}
