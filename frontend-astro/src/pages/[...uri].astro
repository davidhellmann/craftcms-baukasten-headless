---
export async function getStaticPaths() {
    const response = await fetch('http://content.baukasten.test/api', {
        method: 'POST',
        body: JSON.stringify({
            query: `
        query getPages($section:[String]!) {
            pages: entries(section: $section){
              id
              title
              uri
              slug
              typeHandle
              sectionHandle
            }
          }
        `,
            variables: {
                section: ['pages']
            }
        }),
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer KOTwKkTfXNRzI_u2gQPBO7OfyxXoCihi`,
        },
    }).then(response => response.json());


    const { data: { pages } } = await response

    return pages.map((item: any) => {
        return {
            params: { uri: item.uri },
            props: { item },
        };
    });
}

const { uri } = Astro.params;
const { item } = Astro.props;
---

<h1>{uri}: {item.title}</h1>