query entryQuery($section: [String]!, $uri: [String]!, $site: [String]!) {
  entry(section: $section, uri: $uri, site: $site) {
    id
    title
    uri
    slug
    typeHandle
    sectionHandle
    siteId
  }
}
