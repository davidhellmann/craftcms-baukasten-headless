query navigation($site: [String]!, $navHandle: String!, $level: Int = 1) {
  navigationMain: navigationNodes(site: $site, navHandle: $navHandle, level: $level) {
    id
    nodeUri
    siteId
    classes
    title
    customAttributes {
      attribute
      value
    }
    newWindow
    urlSuffix
  }
}
