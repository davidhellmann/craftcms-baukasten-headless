query entry($slug: [String]!, $site: [String]!, $section: [String]!) {
  entry(slug: $slug, site: $site, section: $section) {
    id
    title
    typeHandle
    sectionHandle
    siteId
    slug
  }
}
