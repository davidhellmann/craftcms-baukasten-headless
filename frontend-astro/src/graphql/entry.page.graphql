query page(
  $section: [String]! = ["pages", "news"]
  $uri: [String]!
  $site: [String]!
) {
  page: entry(section: $section, uri: $uri, site: $site) {
    id
    title
    uri
    slug
    typeHandle
    sectionHandle
    siteId
  }
}
