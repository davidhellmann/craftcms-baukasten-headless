query entriesAll($site: [String]!, $section: [String]!) {
  entriesAll: entries(site: $site, section: $section) {
    id
    title
    url
    uri
    typeHandle
    sectionHandle
    siteId
    slug
  }
}
