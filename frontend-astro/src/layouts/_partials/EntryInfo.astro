---
const { siteHandle, sectionHandle, typeHandle, uri, id, title } = Astro.props;
---

<div class="wrapper">
  <ul>
    <li>Site: <span>{siteHandle}</span></li>
    <li>Section: <span>{sectionHandle}</span></li>
    <li>EntryType: <span>{typeHandle}</span></li>
    <li>URI: <span>{uri}</span></li>
    <li>ID: <span>{id}</span></li>
  </ul>
</div>

<style>
  .wrapper {
    position: fixed;
    left: 1rem;
    bottom: 1rem;
    background-color: #fff;
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
    border-radius: 1000px;
    padding: 0.5rem;
    font-size: 13px;
  }

  .wrapper ul {
    display: flex;
    flex-flow: row wrap;
    gap: 0.25rem;
  }

  .wrapper ul li {
    border-radius: 100vw;
    background-color: rgb(251, 230, 255);
    color: rgb(94, 54, 103);
    padding: 0.25rem 0.5rem;
    font-weight: 400;
  }

  .wrapper ul li span {
    font-weight: 600;
  }
</style>
